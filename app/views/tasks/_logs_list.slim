- @task.logs.by_instance.each do |header, logs|
  = logs_list(logs) do
    - if header.finished_at
      | Completed #{about_time(header.finished_at)}. Took #{header.duration.human_time}
    - else
      .text-color Currently Running
      
.text-center = paginate @task.logs
